<Page
    x:Class="SoundByte.UWP.Views.Me.AccountView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:stateTriggers="using:SoundByte.UWP.StateTriggers"
    Transitions="{StaticResource MainTransition}">

    <!-- Main Page -->
    <Grid Background="{ThemeResource ShellBackground}" x:Name="RootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DeviceStates">
                <VisualState x:Name="Xbox">
                    <VisualState.StateTriggers>
                        <stateTriggers:DeviceStateTrigger DeviceFamily="Windows.Xbox"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="Transparent"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <WebView x:Name="LoginWebView"
                 Canvas.ZIndex="5"
                 Visibility="Collapsed"
                 
                 HorizontalAlignment="Stretch"
                 NavigationStarting="LoginWebView_OnNavigationStarting"
                 DefaultBackgroundColor="Transparent"
                 VerticalAlignment="Stretch" />
        
        <ScrollViewer HorizontalAlignment="Stretch"
                      VerticalScrollBarVisibility="Hidden">
            <StackPanel x:Name="StackPanel" Padding="50,0">
                <TextBlock x:Name="TextHeader" Text="Manage Connected Accounts" FontWeight="Bold" Opacity="0.8" FontSize="55" Margin="0,30,0,0" />
                <TextBlock Opacity="0.7" FontSize="14" Margin="0,10,0,12" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Manage your connected SoundCloud and Fanburst Account." />

                <GridView IsItemClickEnabled="False"
                          IsHoldingEnabled="False"
                          IsTapEnabled="False">
                    <Button Padding="0" Margin="0" BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}" BorderThickness="1" Click="ToggleSoundCloud">
                        <StackPanel Orientation="Vertical"                                    
                                    Margin="0" 
                                    Width="170"  
                                    Padding="0,0,0,0"
                                    Background="{ThemeResource ShellNavigationBackground}">
                            <!-- Icon -->
                            <!-- Description and button-->
                            <TextBlock Text="SoundCloud" Opacity="0.8" Padding="10,40" TextAlignment="Center" FontSize="18" FontWeight="SemiBold" />

                            <Grid Background="{ThemeResource SystemControlHighlightAccentBrush}" Padding="10,15">
                                <TextBlock x:Name="SoundCloudText" Text="Login" TextAlignment="Center" FontSize="16" RequestedTheme="Dark" />
                            </Grid>
                        </StackPanel>
                    </Button>

                    <Button Padding="0" Margin="0" BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}" BorderThickness="1" Click="ToggleFanburst">
                        <StackPanel Orientation="Vertical"                                    
                                    Margin="0" 
                                    Width="170"  
                                    Padding="0,0,0,0"
                                    Background="{ThemeResource ShellNavigationBackground}">
                            <!-- Icon -->
                            <!-- Description and button-->
                            <TextBlock Text="Fanburst" Opacity="0.8" Padding="10,40" TextAlignment="Center" FontSize="18" FontWeight="SemiBold" />

                            <Grid Background="{ThemeResource SystemControlHighlightAccentBrush}" Padding="10,15">
                                <TextBlock x:Name="FanburstText" Text="Login" TextAlignment="Center" FontSize="16" RequestedTheme="Dark" />
                            </Grid>
                        </StackPanel>
                    </Button>

                </GridView>
            </StackPanel>
        </ScrollViewer>

        

        <Grid x:Name="LoadingSection" Canvas.ZIndex="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Visibility="Collapsed" Background="{ThemeResource ShellNavigationBackground}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressRing IsActive="True" Width="80" Height="80" />
                <TextBlock TextAlignment="Center" Text="Loading..." FontSize="28" FontWeight="SemiBold" Margin="0,10,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
